 #include<stdio.h>
    int main(){
                   int width, length, area;
                   printf("Give the width of Rectangle : ");
                   scanf("%d", &width);
                   printf("\n Give the Length of Rectangle : ");
                   scanf("%d", &length);
                   area = length * width;
                   printf("\n\n The Area of Rectangle is : %d", area);
    return (0);
    }
